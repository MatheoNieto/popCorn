import { CustomFonts } from "../constants";

const textVariants = {
  defaults: {
    color: "black",
    fontFamily: CustomFonts.Primary,
    letterSpacing: 0.049,
    fontSize: 14,
  },
  titleOfHeader: {},
};

export default textVariants;
